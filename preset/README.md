# 预设样例目录

把从 SillyTavern 导出的 JSON 放在这里备份即可，例如：

- `chat-preset.json` — 聊天补全预设
- `worldbook.json` — 世界书
- `character.json` — 角色卡 V2

网页里用「设置 → 导入」选择文件即可，不必改代码。

正式游玩时，建议把天青卡的 Gal 格式世界书一并导入，模型才会稳定输出 `<Gal>`。
